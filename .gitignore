# Ignorer les fichiers liés à Docker qui ne sont pas essentiels
*.log
*.lock

# Symfony
/project/var/
./project/vendor/
./project/.env.local
./project/.env.*.local
/project/.DS_Store
/project/node_modules/

# Docker volumes (si tu montes certains fichiers)
docker-compose.override.yml

# Éventuellement les données si tu as une BDD locale
/db-data/

# Docker database data
docker/postgres/data/

todo.md